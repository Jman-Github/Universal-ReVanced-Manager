* text=auto

# Ensure the Gradle wrapper uses LF endings so CI can execute it
gradlew text eol=lf

# Keep Windows batch wrappers with CRLF endings
*.bat text eol=crlf

# Android and Magisk execute these scripts with /system/bin/sh.
*.sh text eol=lf
app/src/main/assets/root/module.prop text eol=lf
